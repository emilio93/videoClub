
-- 	CLIENTE
-- 		call addCliente(cedula,nombre,apellido1,apellido2,telefono,email,direccion);
--
-- 		call getCliente(idCliente);
-- 		call getClientes(cantidad,pagina);
-- 		call getClientesMorosos();
--
-- 		call updateCliente(idCliente,cedula,nombre,apellido1,apellido2,telefono,email,direccion);
--
-- 		call deleteCliente(idCliente);
--
-- 		call esMoroso(idCliente);
-- 		call getIdFromCedula(cedula);
-- 		call contarPrestamosCliente(idCliente);
--
--  configuracion
--      call addConfiguracion(nombre,valor,tipo,descripcion);
--      call updateConfiguracion(nombre,valor,tipo,descripcion);
--
-- 	PELICULA
-- 		call addPelicula(titulo,direccion,produccion,ano,genero,duracion,sinopsis,cantidad);
--
-- 		call getPelicula(idPelicula);
-- 		call getPeliculas(cantidad,pagina);
-- 		call getNombrePeliculas();
--
-- 		call updatePelicula(idPelicula,titulo,direccion,produccion,ano,genero,duracion,sinopsis,cantidad);
--
-- 		call deletePelicula(idPelicula);
--
-- 	PRESTAMO
-- 		call addPrestamo(idCliente,idPelicula,salida,devolucion);
--
-- 		call getPrestamosCliente(idCliente);
-- 		call getPrestamosPelicula(idPelicula);
-- 		call getPrestamosClientePelicula(idCliente,idPelicula);
--
-- 		call finalizarPrestamo(idPrestamo);
-- 		call reactivarPrestamo(idPrestamo);
